# asha-memory
 Zero-dependency, local-first graph memory engine designed for autonomous AI agents. Built entirely with the Python standard library and embedded SQLite, AshaMemory provides deterministic context recall, temporal metadata, strict scope isolation, and self-healing graph maintenance without cloud vector databases or third-party package bloat.
